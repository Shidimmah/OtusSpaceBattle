version: '3.8'

services:
  database_service:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_DB: main_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d main_db" ]
      interval: 5s
      retries: 5
      timeout: 3s

  database_migrations:
    build: ./services/database_service
    container_name: otus_database_migrations
    depends_on:
      database_service:
        condition: service_healthy
    command: ["python", "init_db.py"]
    volumes:
      - ./common:/app/common

  api_gateway:
    build: ./services/api_gateway
    container_name: otus_api_gateway
    restart: always
    depends_on:
      - auth_service
      - matchmaking_service
      - fleet_management_service
    ports:
      - "80:80"
    volumes:
      - ./common:/app/common

  auth_service:
    build: ./services/auth_service
    container_name: otus_auth_service
    restart: always
    depends_on:
      database_migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgresql://user:password@database_service/main_db
    ports:
      - "8001:8001"
    volumes:
      - ./common:/app/common

  matchmaking_service:
    build: ./services/matchmaking_service
    container_name: otus_matchmaking_service
    restart: always
    depends_on:
      database_migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgresql://user:password@database_service/main_db
    ports:
      - "8002:8002"
    volumes:
      - ./common:/app/common

  fleet_management_service:
    build: ./services/fleet_management_service
    container_name: otus_fleet_management_service
    restart: always
    depends_on:
      database_migrations:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgresql://user:password@database_service/main_db
    ports:
      - "8003:8000"
    volumes:
      - ./common:/app/common

volumes:
  pgdata:
